#pragma once
#include <iostream>
using std::string;
//C. define enum data type DegreeProgram
enum DegreeProgram
{
SECURITY, NETWORK, SOFTWARE
};

const string degreeProgramStrings[]={ "SECURITY", "NETWORK", "SOFTWARE" };
//parallel array for the enum